{
  "name": "node-project",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest",
    "test:e2e": "playwright test",
    "lint": "biome check .",
    "typecheck": "tsc --noEmit",
    "db:migrate": "prisma migrate dev",
    "deploy": "fly deploy",
    "prepare": "husky"
  }
}
